@page
@model Frontend_Project.Pages.VaerktoejsKasse.IndexModel
@{
    ViewData["Title"] = "Værktøjskasse oversigt oversigt";
}

<div class="row">
    <div class="col-4 col-sm-4 col-lg-4 col-xl-4 col-md-4 p-5">
        <div id="orderheadline">
            <h1>Oversigt over værktøjskasser</h1>

            <p>
                <a class="btn btn-secondary shadow" asp-page="/VaerktoejsKasse/Create">Create New</a>
            </p>
        </div>
    </div>
    <div class="col col-sm col-lg col-xl col-md p-5">
        <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th>
                        ID
                    </th>
                    <th>
                        Model
                    </th>
                    <th>
                        Fabrikant
                    </th>
                    <th>
                        Farve
                    </th>
                    <th>
                        Anskaffet
                    </th>
                    <th>
                        Ejes af 
                    </th>
                    <th>
                        Indeholder værktøjerne
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.LocalModels)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Fabrikant)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Farve)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Anskaffet)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EjesAF.ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Vaerktoejer)
                    </td>
                    <td>
                        <a class="btn btn-secondary shadow" asp-page="/VaerktoejsKasse/Details" asp-route-id="@item.id">Choose</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>

    </div>
</div>